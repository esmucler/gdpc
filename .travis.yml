# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: r
r:
  - release
  - devel
cache: packages

matrix:
  allow_failures:
    - os: osx
  include:
    - os: linux
    - os: osx
      latex: false